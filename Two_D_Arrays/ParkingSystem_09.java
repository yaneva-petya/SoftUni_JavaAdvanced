package Excercises.Two_D_Arrays;

import java.util.*;

public class ParkingSystem_09 {
    public static void main(String[] args) {
      /*  Scanner s = new Scanner(System.in);
        String[] matrixSize=s.nextLine().split("\\s+");
        boolean[][] matrix=new boolean
                [Integer.parseInt(matrixSize[0])][Integer.parseInt(matrixSize[1])];

        for(int row=0;row<matrix.length;row++){
            //first column of every row is free;
            matrix[row][0]=true;
        }

        while (true){
            String input=s.nextLine();
            if("stop".equals(input)){break;}

            String[] data=input.split(" ");
            int z=Integer.parseInt(data[0]);//entry row
            int row=Integer.parseInt(data[1]);
            int col=Integer.parseInt(data[2]);
            int distance=Math.abs(z-row)+1;


        }*/
    }
}
